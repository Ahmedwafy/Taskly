// src → app → (pages) → projects → add

import AddNewProjectPage from '@/app/components/pages/AddNewProjectPage';

export default function Add_New_Project() {
  return (
    <>
      <AddNewProjectPage />
    </>
  );
}
