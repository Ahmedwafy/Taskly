// src > app > (pages) > projects > [projectId] > members > invite > page.tsx
// Full page on refresh / direct visit
export default function Default() {
  return (
    <div>
      <h1>Refresh / Direct visit Invite Page</h1>
    </div>
  );
}
